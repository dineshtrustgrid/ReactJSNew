// import {React} from 'react'; 
// import  {ReactDOM} from 'react-dom/client';

import React from "react";
import ReactDOM from 'react-dom/client';

const root = ReactDOM.createRoot(document.getElementById("root"));
const element = <h1>Hello vinod !!! </h1>
root.render(element);

// React and ReactDOM are default parameter should not use within curly braces {} in import statement

In curly braces {} use only named parameters

// Props is read only 
// state is update the data 
